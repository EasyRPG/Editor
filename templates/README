Copy this whole folder into the executable's directory, otherwise new_game will crash
On Windows, replace Player.exe executable with a real EasyRPG Player binary (continuous build) from https://easy-rpg.org/player-downloads.html
